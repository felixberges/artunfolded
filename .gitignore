# volcados de depuración
build_out.txt
salida.txt

# temporales del pipeline 3D
tmp_resized.glb

# modelos originales/intermedios pesados (solo se sube el .glb final)
public/models/**/*.original.glb
public/models/**/*.ktx2.glb

# notas de sesión, no son de la web
claude/

# duplicado por error
src/**/*.css.txt

dist/
node_modules/

# editor/SO
.vscode/
.DS_Store